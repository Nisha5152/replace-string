#include <stdio.h>

int main()
{
    char a[100];
    int i,c=0;
    scanf("%[^\n]",a);
    for(i=0;a[i];i++)
    {
         if(a[i]=='a'||a[i]=='e'||a[i]=='i'||a[i]=='o'||a[i]=='u')
         {
              c++;
              a[i]=c;
         }
         printf("%s",a);
    }
    

    return 0;
}
